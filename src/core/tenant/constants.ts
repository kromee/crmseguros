import { SAAS_PRODUCT_CONFIG } from "./saas-catalog";

/** Slug del plan por defecto (2 usuarios) */
export const DEFAULT_PLAN_SLUG = SAAS_PRODUCT_CONFIG.defaultPlanSlug;

/** Slug del tenant piloto (Seguros Vale) */
export const DEFAULT_TENANT_SLUG = SAAS_PRODUCT_CONFIG.defaultTenantSlug;

export { SAAS_PRODUCT_CONFIG };
